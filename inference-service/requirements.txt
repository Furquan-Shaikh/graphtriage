# Day 1: only what's needed to run a health-check FastAPI app.
# Heavier ML dependencies (torch, torch-geometric, sentence-transformers, shap)
# are added starting Day 3-5 per phases.md - installing them all on Day 1
# would slow down your very first docker build for no benefit yet.
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.4
python-dotenv==1.0.1
neo4j==5.21.0
